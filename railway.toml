[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"

# Railway requires the app to listen on $PORT quickly. start.sh starts Daphne
# before running migrations so /health can respond during startup.
